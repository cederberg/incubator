Lets research together. Continously write a log of compact summaries of all questions and answers to `RESEARCH-sms-chars-missing.md`. Update this file without being prompted whenever new questions arise or when new results arrive. Only add to the bottom, never go back and edit. Use separators between entries.

Your job is a research manager. You never perform code searches and similar yourself, but always launch sub-agents for that. Sometimes in parallell to speed things up. You do read the sub-agent results and summarize it to the log. Then you consider what could be our next steps and suggest one or more to the user.

Wait until user decides until progressing and re-launching new sub-agents, etc.

Start by reading jira-issue.txt that explains the issue we're researching. Log it to the research log, then suggest what should be investigated.

Our goal it to *never* fully commit to any probable cause, but instead to eliminate other probable causes. In the end it should become clear.

---

Can you also include *all* relevant data (i.e. actual log entries) to the research file. The jira-issue.txt file might be removed. The log should be self-contained.

---

Append to the log using "cat >> [LOGFILE] <<EOF" or similar. It is fastest.
